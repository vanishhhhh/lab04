language: cpp

os: linux

compiler:
  - gcc
  - clang

script:
  - cmake -H. -B_build -DCMAKE_INSTALL_PREFIX=_install
  - cmake --build _build
  - cmake --build _build --target install

addons:
  apt:
    packages:
      - cmake
      - cmake-data